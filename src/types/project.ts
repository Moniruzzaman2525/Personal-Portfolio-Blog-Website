export type TProject = {
    _id: string;
    image: string;
    name: string;
    description: string;
    liveLink: string;
    createdAt: string
    status: string
    startDate: string
    endDate: string
    updatedAt: string
}
